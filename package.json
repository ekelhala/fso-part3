{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "copy": "@powershell Copy-Item -Path (Get-Item -Path .\/* -Exclude ('requests','node_modules','.git')).FullName -Destination '../../fullstack_open/part3' -Force"
  },
  "author": "Emil Kelhälä",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
